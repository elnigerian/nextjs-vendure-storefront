import * as React from 'react';
import '../public/static/css/storefront.css';

const StoreFrontApp = ({ Component, pageProps }: any) => {
    return <Component {...pageProps} />;
};

export default StoreFrontApp;
